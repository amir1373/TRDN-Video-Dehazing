# Archived notebooks

This directory holds the original exploratory RunPod notebook. It is retained
read-only for provenance and documents a superseded, flawed evaluation protocol.
Do not clean it, strip its outputs, import from it, or use it to produce reported
results. The authoritative replacements are the scripts in `scripts/`.
